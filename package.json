{
  "name": "node-sqlite-parallelism",
  "version": "1.0.0",
  "description": "Test to see if node-sqlite3 can run queries in parallel",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "benchmark-single": "mkdir -p test-via-script_single_connection/results && UV_THREADPOOL_SIZE=2 node test-via-script_single_connection/benchmark.js | tee test-via-script_single_connection/results/benchmark.txt",
    "benchmark-n-connection": "mkdir -p test-via-script_n_connection/results && UV_THREADPOOL_SIZE=2 node test-via-script_n_connection/benchmark.js | tee test-via-script_n_connection/results/benchmark.txt",
    "benchmark-python": "cd test-via-python-cli && python3 benchmark.py",
    "compare-all": "bash compare-all-results.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^5.0.2"
  }
}